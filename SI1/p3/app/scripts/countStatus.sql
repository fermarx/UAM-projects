EXPLAIN
SELECT COUNT(*)
FROM ORDERS
WHERE STATUS is null;

EXPLAIN
SELECT COUNT(*)
FROM ORDERS
WHERE STATUS ='Shipped';

CREATE INDEX status_orders ON ORDERS(STSTUS);

EXPLAIN
SELECT COUNT(*)
FROM ORDERS
WHERE STATUS is null;

EXPLAIN
SELECT COUNT(*)
FROM ORDERS
WHERE STATUS ='Shipped';

ANALYZE;

EXPLAIN
SELECT COUNT(*)
FROM ORDERS
WHERE STATUS is null;

EXPLAIN
SELECT COUNT(*)
FROM ORDERS
WHERE STATUS='Shipped';

EXPLAIN 
SELECT COUNT(*)
FROM ORDERS
WHERE STATUS ='Paid';

EXPLAIN
SELECT COUNT(*)
FROM ORDERS
WHERE STATUS ='Processed';